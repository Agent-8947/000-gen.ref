# Environment Variables for DNA Matrix Builder

# App Mode
# Set to "production" to enable viewer mode (read-only, no editor UI)
# Leave empty or set to "development" for editor mode
VITE_APP_MODE=development

# API Keys (if needed)
# VITE_API_KEY=your_api_key_here
# GEMINI_API_KEY=your_gemini_key_here
